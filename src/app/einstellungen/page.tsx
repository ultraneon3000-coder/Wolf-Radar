// Platzhalter — Inhalt (Urteils-Schwelle, Kategorie-Verwaltung, Standard-Sortierung,
// Videos pro Suche, Proxy-URL, "Meine Korrekturen") folgt in der nächsten Ausbaustufe.
export default function EinstellungenPage() {
  return (
    <main className="mx-auto flex w-full max-w-6xl flex-1 flex-col items-center justify-center gap-3 px-4 py-10 text-center sm:px-6">
      <h1 className="text-xl font-bold tracking-tight text-ink">Einstellungen</h1>
      <p className="text-sm text-muted">Wird als Nächstes gebaut.</p>
    </main>
  );
}
